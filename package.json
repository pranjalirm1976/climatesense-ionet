{
  "name": "climate-sense-ionet",
  "version": "1.0.0",
  "description": "Hackathon Fullstack Setup",
  "scripts": {
    "server": "cd server && node server.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}